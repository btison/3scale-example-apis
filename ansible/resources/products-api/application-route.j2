kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: {{ application_name }}
spec:
  to:
    kind: Service
    name: {{ application_name }}
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
